apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: admin-service
  ports:
  - port: 3014
    targetPort: 3014
  type: ClusterIP