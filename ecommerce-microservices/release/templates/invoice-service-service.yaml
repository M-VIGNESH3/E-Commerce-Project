apiVersion: v1
kind: Service
metadata:
  name: invoice-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: invoice-service
  ports:
  - port: 3010
    targetPort: 3010
  type: ClusterIP