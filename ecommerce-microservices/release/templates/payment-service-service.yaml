apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: payment-service
  ports:
  - port: 3009
    targetPort: 3009
  type: ClusterIP