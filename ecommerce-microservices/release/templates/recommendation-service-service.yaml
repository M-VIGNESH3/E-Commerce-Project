apiVersion: v1
kind: Service
metadata:
  name: recommendation-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: recommendation-service
  ports:
  - port: 3013
    targetPort: 3013
  type: ClusterIP