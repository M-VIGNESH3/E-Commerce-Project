apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: shipping-service
  ports:
  - port: 3011
    targetPort: 3011
  type: ClusterIP